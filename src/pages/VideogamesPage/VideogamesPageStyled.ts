import styled from "styled-components";

const VideogamesPageStyled = styled.div`
  background-color: ${({ theme }) => theme.colors.light_color};
  padding-bottom: 110px;
`;

export default VideogamesPageStyled;
